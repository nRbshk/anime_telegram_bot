from setuptools import setup

setup(
   name='telegram bot with python flask server',
   version='1.0',
   description='Useful module for chrome extension',
   author='nRbshk',
   author_email='n.ryaboshchuK@gmail.com',
   packages=['telegram bot with python flask server'],
   install_requires=['flask', 'flask-cors', 'aiogram', 'requests', 'bs4'],
)